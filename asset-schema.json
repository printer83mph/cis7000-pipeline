{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "CIS-7000 Asset Metadata v1.3",
  "description": "An asset's metadata for use in CIS-7000 Production Pipeline, SP2024",
  "properties": {
    "assetName": {
      "description": "The asset's name, in camelCase",
      "type": "string"
    },
    "author": {
      "description": "The original asset author's Pennkey",
      "type": "string"
    },
    "historical": {
      "description": "All historical modifications to this asset, including the original author's creation.\nIn chronological order, with the most recent modification first.",
      "type": "array",
      "items": {
        "type": "object",
        "description": "A modification to this asset",
        "properties": {
          "date": {
            "description": "The date of this modification",
            "type": "string",
            "format": "date"
          },
          "author": {
            "type": "string",
            "description": "The Pennkey of the author of this modification"
          },
          "changes": {
            "description": "A brief description of the changes made in this modification",
            "type": "string"
          },
          "version": {
            "description": "Major and minor version numbers of this iteration, i.e. 1.0",
            "type": "string",
            "pattern": "^[0-9]+\\.[0-9]+$"
          },
          "schemaVersion": {
            "description": "Version of the asset schema this iteration uses",
            "type": "string",
            "pattern": "^[0-9]+\\.[0-9]+$"
          }
        },
        "required": ["date", "author", "changes", "version", "schemaVersion"]
      },
      "minItems": 1,
      "uniqueItems": true
    },
    "version": {
      "description": "Major and minor version of this asset, i.e. 1.0",
      "type": "string",
      "pattern": "^[0-9]+\\.[0-9]+$"
    },
    "keywords": {
      "description": "Key words or phrases associated with this asset",
      "type": "array",
      "items": {
        "type": "string"
      },
      "minItems": 1,
      "uniqueItems": true
    }
  },
  "required": [
    "assetName",
    "author",
    "historical",
    "version"
  ]
}